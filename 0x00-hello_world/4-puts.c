#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char STRING[64] = "\"Programming is like building a multilingual puzzle";

	puts(STRING);

	return (0);

}
