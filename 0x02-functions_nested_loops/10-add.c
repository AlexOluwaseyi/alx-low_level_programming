#include <stdio.h>
#include "main.h"

/**
 * add - adds up tow integers
 * @x: one of two integers to be added
 * @y: one of two integers to be added
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
