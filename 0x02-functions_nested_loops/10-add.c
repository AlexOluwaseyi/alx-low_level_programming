#include <stdio.h>
#include "main.h"

/**
 * add - adds up tow integers
 * @x: one of two integers to be added
 * @y: one of two integers to be added
 *
 * Return: sum of the two number
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
