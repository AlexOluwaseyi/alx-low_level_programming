
#include "shell.h"

/**
 * get_status - gets the  value of global variable status
 * Return: status
 */

int get_status(void)
{
	int status = 0;

	return (status);
}

/**
 * set_status - sets new value of global variable status
 * @n_status - new value to be assigned to status
 */

void set_status(int new_status)
{
	int status;

	status = new_status;
	status += 0;
}
